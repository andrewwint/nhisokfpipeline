# venv + caches
.venv/
__pycache__/
*.pyc

# pipeline outputs + fetched data (regenerated by a run)
build/
data/
*.parquet

# notebook checkpoints
.ipynb_checkpoints/

# the local MWAA runner is a separate clone, not part of this repo
aws-mwaa-local-runner/
